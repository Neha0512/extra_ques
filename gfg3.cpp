
#include<bits/stdc++.h>
using namespace std;
int noofdigits()
{

 long long factorial(int N) 
        long sum=1;
        for(int i=2;i<=N;i++)
             sum=sum*i;
            return sum ;
        int count=0;
        if(sum!=0)
        sum=sum/10;
        count++;
        cout<<count;
    
};
